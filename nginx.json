{
    "id": "mlh", 
    "container": {
        "docker": {
            "image": "nginx",
            "network": "BRIDGE",
            "portMappings": [
                { "containerPort": 80, "hostPort": 80, "protocol": "tcp" }
            ]
        },
        "type": "DOCKER"
    },
    "cpus": 0.5,
    "mem": 512,
    "instances": 1
}
